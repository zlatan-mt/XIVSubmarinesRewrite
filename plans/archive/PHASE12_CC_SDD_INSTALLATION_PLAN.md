# Phase 12: cc-sdd 導入実施計画

## 概要

XIV Submarines Rewrite プロジェクトに **cc-sdd（Spec-Driven Development）** を導入し、
**Cursor IDE**、**Claude Code**、**Codex CLI** の3ツール統合環境を構築します。

---

## 🎯 導入目標

1. **仕様駆動開発の確立**: 要件→設計→タスク→実装の構造化ワークフロー
2. **3ツール統合**: Cursor/Claude Code/Codex で統一されたコマンド体系
3. **プロジェクトメモリ**: 文脈を保持する共有知識ベース
4. **既存ワークフローとの融合**: Phase 計画との整合性維持

---

## 📅 実施スケジュール

### Phase 12-A: 準備と評価（1日）
- cc-sdd 機能評価
- ディレクトリ構造設計
- dry-run での動作確認

### Phase 12-B: 導入（1日）
- 3ツールのインストール
- ドキュメント統合
- ステアリング初期化

### Phase 12-C: 試験運用（3-5日）
- パイロットプロジェクト実施
- ワークフロー検証
- ドキュメント調整

---

## 🔧 Phase 12-A: 準備と評価

### タスク一覧

#### 1. 事前確認
```bash
# 現在のブランチ確認
git branch --show-current
# → develop であることを確認

# 作業ディレクトリクリーン確認
git status
# → nothing to commit, working tree clean
```

#### 2. dry-run での動作確認

**Cursor IDE**:
```bash
npx cc-sdd@latest --cursor --lang ja --kiro-dir plans/specs --dry-run
```

**Claude Code**:
```bash
npx cc-sdd@latest --claude-code --lang ja --kiro-dir plans/specs --dry-run
```

**Codex CLI**:
```bash
npx cc-sdd@latest --codex --lang ja --kiro-dir plans/specs --dry-run
```

#### 3. 出力内容の確認

**期待される出力**:
```
✓ .cursor/commands/kiro/ (11コマンド)
✓ .claude/commands/kiro/ (11コマンド)
✓ .codex/prompts/ (11プロンプト)
✓ plans/specs/.kiro/settings/
✓ plans/specs/.kiro/steering/
✓ AGENTS.md (Cursor/Codex用)
✓ CLAUDE.md (Claude Code用)
```

#### 4. 既存ファイルとの競合チェック

```bash
# 既存 AGENTS.md のバックアップ
cp AGENTS.md AGENTS.md.backup

# 既存ディレクトリの確認
ls -la | grep -E "\.cursor|\.claude|\.codex"
```

---

## 🚀 Phase 12-B: 導入

### 実施手順

#### Step 1: バックアップ作成

```bash
# 重要ファイルのバックアップ
git checkout develop
git pull origin develop

# ブランチ作成
git checkout -b feature/phase12-cc-sdd-integration

# 現在の状態を記録
git log --oneline -5 > pre-sdd-state.txt
```

#### Step 2: Cursor IDE インストール

```bash
npx cc-sdd@latest --cursor --lang ja --kiro-dir plans/specs --backup
```

**確認**:
- `.cursor/commands/kiro/` にコマンドが配置されたか
- `AGENTS.md` が生成または更新されたか

#### Step 3: Claude Code インストール

```bash
npx cc-sdd@latest --claude-code --lang ja --kiro-dir plans/specs --backup
```

**確認**:
- `.claude/commands/kiro/` にコマンドが配置されたか
- `CLAUDE.md` が生成されたか

#### Step 4: Codex CLI インストール

```bash
npx cc-sdd@latest --codex --lang ja --kiro-dir plans/specs --backup
```

**確認**:
- `.codex/prompts/` にプロンプトが配置されたか
- `AGENTS.md` が更新されたか（Cursor分と統合）

#### Step 5: ドキュメント統合

**AGENTS.md の統合**:
```bash
# 1. バックアップから既存ルールを抽出
# 2. cc-sdd生成のAGENTS.mdとマージ
# 3. 統合版を作成

# 既存の重要セクションを保持:
# - Repository Guidelines
# - コーディングスタイル
# - テスト指針
# - セキュリティと設定の注意

# cc-sddのセクションを追加:
# - Spec-Driven Development ワークフロー
# - 主要コマンド
# - 使用例
```

**マージ推奨構成**:
```markdown
# AGENTS.md 構成案

## Repository Guidelines（既存）
## Spec-Driven Development ワークフロー（新規）
## 主要コマンド（新規）
## コーディングスタイルと命名（既存）
## テスト指針（既存）
## 使用例（新規）
## セキュリティと設定の注意（既存）
```

#### Step 6: ステアリング初期化

**プロジェクトメモリの作成**:
```bash
# Cursor または Claude Code で実行
/kiro:steering
```

**ステアリング初期内容**:
```markdown
# XIV Submarines Rewrite プロジェクトメモリ

## アーキテクチャ
- Dalamud プラグイン（API Level 13）
- .NET 9.0-windows ターゲット
- レイヤー構成: Presentation → Application → Domain → Infrastructure

## 技術スタック
- UI: ImGui (Dalamud.Bindings.ImGui)
- テスト: xUnit (.NET), Playwright (E2E)
- 通知: Discord/Notion Webhooks

## 開発原則
1. ファイルサイズ300行以下
2. 型ヒント徹底（警告ゼロ）
3. テストカバレッジ90%以上
4. DRY原則の遵守

## ブランチ戦略
- develop: 開発用（全ファイル）
- release: 公開用（開発資料除外）
- feature/*: 機能ブランチ

## 命名規則
- クラス: PascalCase
- メソッド: PascalCase
- ローカル変数: camelCase
- 定数: SCREAMING_SNAKE_CASE

## 現在の Phase
- Phase 11 完了: リポジトリクリーンアップとブランチ分離
- Phase 12 進行中: cc-sdd 導入
```

#### Step 7: .gitignore 更新

```bash
# plans/specs/.kiro/steering/ を develop のみで追跡
# release ブランチでは除外

# .gitignore に追加（既にある場合はスキップ）
echo "" >> .gitignore
echo "# cc-sdd local cache" >> .gitignore
echo ".kiro/cache/" >> .gitignore
```

#### Step 8: コミット

```bash
git add .cursor/ .claude/ .codex/ plans/specs/ AGENTS.md CLAUDE.md .gitignore
git status

git commit -m "feat: integrate cc-sdd for Cursor, Claude Code, and Codex

- Add Spec-Driven Development workflow
- Install cc-sdd commands for 3 AI tools
- Create project memory (steering)
- Integrate SDD workflow with existing Phase development

Related: Phase 12-B"
```

---

## 🧪 Phase 12-C: 試験運用

### パイロットプロジェクト

**テーマ**: 小規模な機能追加または改善

**推奨候補**:
1. 通知バッチングの細かい調整
2. Overview UI の軽微な改善
3. 新しいユーティリティ関数の追加

### 実施手順

#### ステップ1: プロジェクトメモリ確認

```bash
# Cursor または Claude Code で実行
/kiro:steering

# 内容を確認し、必要に応じて更新
```

#### ステップ2: 新機能の初期化

```bash
/kiro:spec-init <機能の簡潔な説明>

# 例:
/kiro:spec-init Add tooltip to notification retry button showing retry count and last attempt time
```

#### ステップ3: 要件定義

```bash
/kiro:spec-requirements tooltip-retry-info

# 生成された要件文書を確認:
# plans/specs/requirements/tooltip-retry-info.md
```

**レビューポイント**:
- [ ] 機能要件は明確か
- [ ] 非機能要件（パフォーマンス、UI）は記載されているか
- [ ] Dalamud/ImGui の制約は考慮されているか

#### ステップ4: 設計

```bash
/kiro:spec-design tooltip-retry-info

# Claude Code で実行推奨（詳細な設計分析）
```

**レビューポイント**:
- [ ] 既存アーキテクチャとの整合性
- [ ] UI レスポンシブ対応
- [ ] テスト戦略の明確化

#### ステップ5: タスク分解

```bash
/kiro:spec-tasks tooltip-retry-info -y
```

**レビューポイント**:
- [ ] タスクは1-3日で完了可能か
- [ ] 依存関係は明確か
- [ ] テストタスクは含まれているか

#### ステップ6: 実装

```bash
# タスク1.1を実装
/kiro:spec-impl tooltip-retry-info 1.1

# Cursor IDE で実装作業

# テスト実行
dotnet test --filter "RetryButton"

# タスク1.2を実装
/kiro:spec-impl tooltip-retry-info 1.2
```

#### ステップ7: 完了レポート

```bash
# 完了したタスクの記録
# plans/specs/completion/tooltip-retry-info.md を作成

# または従来のPhase完了レポート形式でもOK
```

---

## ✅ 検証項目

### 機能検証
- [ ] 3ツール（Cursor/Claude Code/Codex）でコマンドが動作する
- [ ] プロジェクトメモリが共有されている
- [ ] 仕様書が正しく生成される
- [ ] 既存の開発ワークフローを阻害しない

### ドキュメント検証
- [ ] AGENTS.md が既存ルールと cc-sdd を統合している
- [ ] CLAUDE.md が Claude Code 固有の使い方を説明している
- [ ] ステアリングがプロジェクトの文脈を正確に反映している

### ワークフロー検証
- [ ] 要件→設計→タスク→実装の流れがスムーズ
- [ ] Phase 計画との併用が可能
- [ ] develop/release ブランチ戦略と競合しない

---

## 🔄 ロールバック手順

問題が発生した場合の復旧手順：

```bash
# Step 1: 機能ブランチを破棄
git checkout develop
git branch -D feature/phase12-cc-sdd-integration

# Step 2: バックアップから復元
cp AGENTS.md.backup AGENTS.md

# Step 3: 追加ファイルを削除
rm -rf .cursor/commands/kiro/
rm -rf .claude/commands/kiro/
rm -rf .codex/prompts/
rm -rf plans/specs/.kiro/
rm CLAUDE.md

# Step 4: 元の状態を確認
git status
# → すべてクリーンになっていることを確認
```

---

## 📊 成功基準

### 定量的指標
- [ ] パイロットプロジェクトで要件→設計→タスク→実装を完走
- [ ] 生成された仕様書の品質が既存 Phase 計画と同等以上
- [ ] 3ツールでコマンドが正常動作（エラー率 < 5%）

### 定性的指標
- [ ] 開発者の満足度（ワークフローの改善を実感）
- [ ] ドキュメントの可読性・保守性向上
- [ ] Phase 計画との併用が自然にできる

---

## 📝 次のステップ

Phase 12-C 完了後：

1. **Phase 13 で本格運用**
   - cc-sdd ワークフローを標準採用
   - すべての新機能で仕様駆動開発を実施

2. **テンプレートカスタマイズ**
   - プロジェクト固有の要件テンプレート作成
   - 設計レビューチェックリスト整備

3. **チーム展開**
   - 他の開発者への導入サポート
   - ベストプラクティス共有

---

## 🎯 期待される効果

### 短期効果（1-2週間）
- 仕様書の品質向上
- 設計レビューの効率化
- タスク見積もり精度向上

### 中期効果（1-2ヶ月）
- 実装の手戻り削減
- ドキュメントの一貫性向上
- Phase 計画の質向上

### 長期効果（3ヶ月以上）
- プロジェクトメモリによる文脈継続性
- 新規参加者のオンボーディング効率化
- 再利用可能な設計パターン蓄積

---

**実施責任者**: 開発チーム  
**開始予定日**: 2025-10-26  
**完了予定日**: 2025-11-01  
**ステータス**: 準備完了

